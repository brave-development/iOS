# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Halla' do
	use_frameworks!

	pod 'HMSegmentedControl'
	pod 'CustomBadge'
	#pod 'PubNub'
	pod 'Mapbox-iOS-SDK'
	pod 'Parse', '~> 1.14'
    pod 'SwiftyJSON'

end
